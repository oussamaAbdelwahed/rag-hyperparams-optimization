to be able to run the project,you must provide the env vars:
PINECONE_API_KEY
PINECONE_INDEX_BASE_NAME=ats-chrono-rag-hyperparams-optim (index dosnt have to exist in pinecone, if doesnt exist, we create it automatically)

we are interested on running script ats_async_find_opt_params_sync_pinecone.py

you must have doc file and eval....json file at the root of the project
